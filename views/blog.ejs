<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head') %>
    <title>Blog Page</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        min-height: 100vh;
        background-color: black;
        background-image: url("https://res.cloudinary.com/dzfkistam/image/upload/v1736862343/rb_4197_pbjapt.png");
        background-position: center;
        background-size: cover;
        background-attachment: fixed;
        background-repeat: no-repeat;
        color: #e0e0e0;
        font-family: "Poppins", sans-serif;
      }

      .container {
        display: flex;
        flex-direction: row;
        justify-content: center;
        align-items: flex-start;
        padding: 60px 20px;
        min-height: 100vh;
        gap: 40px;
        overflow: hidden;
      }

      .card {
        background: #1e1e1e;
        border: 1px solid #333;
        border-radius: 15px;
        overflow: hidden;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        width: 700px;
        max-width: 100%;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
      }

      .card:hover {
        transform: translateY(-5px);
        filter: drop-shadow(0 0 0.5em #aa4c8b);
      }

      .card img {
        width: 100%;
        object-fit: cover;
      }

      .card-body {
        padding: 40px;
        text-align: left;
      }

      .card-title {
        font-size: 3rem;
        margin-bottom: 20px;
        color: #fff;
      }

      .card-text {
        color: #b0b0b0;
        font-size: 1.3rem;
        line-height: 1.8;
        margin-bottom: 20px;
      }

      .author-text {
        font-size: 1rem;
        margin-top: 30px;
        font-style: italic;
        color: #aaa;
      }

      .btn-primary1 {
        background-color: #a92c5e;
        border: none;
        padding: 14px 30px;
        border-radius: 10px;
        font-size: 1rem;
        display: inline-block;
        margin-top: 30px;
        color: #fff;
        text-decoration: none;
        transition: all 0.3s ease, transform 0.2s ease;
      }

      .btn-primary1:hover {
        background-color: #be316a;
        filter: drop-shadow(0 0 0.5em #aa4c8b);
        /* box-shadow: #6d3159 0px 0px 30px 5px; */
        transform: translateY(-3px);
      }

      .comments-section {
        width: 400px;
      }

      .comments-card,
      .form-card {
        background: #1e1e1e;
        border: 1px solid #333;
        border-radius: 15px;
        padding: 30px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.4);
        margin-bottom: 20px;
      }

      .comment {
        width: 100%;
        background: #252525;
        border-radius: 10px;
        padding: 15px;
        margin-bottom: 15px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.4);
      }

      .comment p {
        margin: 0;
        font-size: 1rem;
        color: #ddd;
      }

      .comment .comment-author {
        margin-top: 8px;
        font-size: 0.85rem;
        color: #aaa;
      }

      /* Adjustments for the Video Link */
      .card-text a {
        color: #9d4780;
        text-decoration: none;
        display: inline-block;
        margin-left: 5px;
      }

      .card-text a:hover {
        color: #aa4c8b;
        text-decoration: underline;
      }

      .body-text {
        overflow: hidden;
        text-align: justify;
        white-space: pre-wrap;
        line-height: 1.6;
        font-size: 1rem;
        color: #fff;
        margin: 10px 0;
      }

      /* Responsive Styles */
      @media (max-width: 1024px) {
        .card-body {
          padding: 20px;
        }

        .card-title {
          font-size: 2.5rem;
        }

        .card-text {
          font-size: 1.2rem;
        }

        .author-text {
          font-size: 1rem;
        }

        .btn-primary1 {
          padding: 12px 25px;
          font-size: 0.9rem;
        }

        .comments-section {
          width: 85%;
        }

        .form-card,
        .comments-card {
          width: 100%;
          padding: 20px;
        }

        .comment p {
          font-size: 1rem;
        }
      }

      @media (max-width: 768px) {
        .container {
          width: 100%;
          flex-direction: column;
          justify-content: center;
          align-items: center;
          /* padding: 20px; */
        }
        .card {
          width: 80%;
        }
        .card-body {
          padding: 15px;
        }

        .card-title {
          font-size: 2rem;
        }

        .card-text {
          font-size: 1.1rem;
        }

        .author-text {
          font-size: 0.9rem;
        }

        .btn-primary1 {
          padding: 10px 20px;
          font-size: 0.9rem;
        }
        .comments-section {
          width: 80%;
        }

        .comment p {
          font-size: 0.9rem;
        }
      }

      @media (max-width: 480px) {
        .container {
          flex-direction: column;
          display: flex;
          justify-content: center;
          align-items: center;
          /* overflow: auto; */
          width: 400px;
        }

        .card {
          background: #1e1e1e;
          border: 1px solid #333;
          border-radius: 15px;
          /* overflow: auto; */
          transition: transform 0.3s ease, box-shadow 0.3s ease;
          width: 300px;
          box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
        }
        .card-body {
          /* overflow: auto; */
          width: 70%;
        }
        .body-text {
          text-align: start;
          white-space: pre-wrap;
          color: #fff;
          font-size: 0.9rem;
          line-height: 1.6;
          margin: 10px 5px;
        }
        .card-title {
          font-size: 1.5rem;
        }

        .card-text {
          font-size: 1rem;
        }

        .btn-primary1 {
          width: 100%;
        }

        .comment p {
          font-size: 0.85rem;
        }
      }
    </style>
  </head>
  <body>
    <%- include("./partials/navbar") %> <% if (locals.blog) { %>
    <div class="container">
      <div>
        <div class="card">
          <img
            src="<%= blog.coverImgUrl %>"
            class="card-img-top"
            alt="Blog Image"
          />
          <div class="card-body">
            <h1 class="card-title"><%= blog.title %></h1>
            <p class="card-text body-text" style="color: white">
              <%= blog.body %>
            </p>

            <p class="card-text">
              Video:
              <a target="_blank" href="<%= blog.videoLink %>">
                <%= blog.videoLink %>
              </a>
            </p>

            <p class="author-text">Written by <%= blog.createdBy.name %></p>
          </div>
        </div>
      </div>

      <div class="comments-section">
        <div class="form-card">
          <form action="/blog/comment/<%= blog._id %>" method="post">
            <div>
              <input
                type="text"
                name="content"
                class="form-control"
                id="comment"
                placeholder="Enter your Comment"
                required
              />
            </div>
            <button type="submit" class="btn-primary1 w-100">
              Submit Comment
            </button>
          </form>
        </div>

        <% if (locals.comments[0]) { %>
        <div class="comments-card">
          <h3
            style="color: #fff; font-weight: 700; text-align: center"
            class="mb-3"
          >
            Total Comments: <%= comments.length %>
          </h3>
          <% comments.forEach(comment => { %>
          <div class="comment">
            <p><%= comment.content %></p>
            <p class="comment-author">by <%= comment.createdBy.name %></p>
          </div>
          <% }) %>
        </div>
      </div>
      <% } %>
    </div>
    <% } %> <%- include('./partials/scripts') %>
  </body>
</html>
